package com.numerator.automation.Utils;

import org.openqa.selenium.*;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * @Todo Implementation of wait method specific to the application behaviour
 *
 * @author Nitin Chaturvedi
 */
public class WebdriverUtils {

    private static final Logger log = LoggerFactory.getLogger(WebdriverUtils.class);

    public static void waitForPageLoaded(WebDriver driver) {
    }

    public static void waitForElementToBeClickable(WebDriver driver, WebElement userNameField) {
    }
}
